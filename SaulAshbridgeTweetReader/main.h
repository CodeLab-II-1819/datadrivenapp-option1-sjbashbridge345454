#pragma once
#include <iostream>
#include <fstream>
#include <string>

//Declares functions 'guide', 'optionSelect', 'output' and 'results'
void guide();
int optionSelect(char count);
void output();
void results();